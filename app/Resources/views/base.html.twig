<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Muser{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" type="text/stylesheet" href="http://code.ionicframework.com/ionicons/2.0.1/css/ionicons.min.css">
            <style>
                body {
                    font-family: Arial;
                    background-color: #DDD;
                    margin: 0;
                    padding: 0;
                    height: calc(100vh - 60px);
                }

                #page-wrapper > header {
                    position: fixed;
                    top: 0px;
                    width: 100%;
                    box-sizing: border-box;
                    z-index: 10;
                    box-shadow: 0 0 10px rgba(0, 0, 0, .5);
                }

                #title-bar {
                    height: 31px;
                    background-color: #585;
                    color: #fff;
                    width: 100%;
                    box-sizing: border-box;
                    z-index: 12;
                }

                #page-wrapper > header h1 {
                    margin: 0;
                    padding: 0;
                    text-align: center;
                    letter-spacing: 2px;
                    font-size:26px;
                    font-weight: normal;
                }
                footer p {
                    text-align: center;
                    color: #999;
                    font-style: italic;
                    font-size: .8em;
                }
                footer a {
                    font-style: normal;
                    color: #585;
                }
                #subnav {
                    width: 100%;
                    height: 30px;
                    background-color: white;
                }
                #subnav ul {
                    margin: 0 auto;
                    padding: 0;
                    list-style-type: none;
                    width: 70%;
                }
                #subnav ul li {
                    float: left;
                    display: block;
                    width: 100%;
                    padding-top: 8px;
                    font-size: 10pt;
                    text-align: center;
                    text-transform: uppercase;
                }
                #subnav ul li a {
                    color: #333;
                    text-decoration: none;
                }

                #page-wrapper {
                    min-height:100%;
                    position:relative;
                    padding-top: 60px;
                }
                #content-body {
                    padding-bottom: 70px;
                }
                footer {
                    width: 100%;
                    height: 70px;
                    position: absolute;
                    bottom: 0;
                    left: 0;
                    background-color: rgba(0,0,0,.05);
                }
            </style>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body><div id="page-wrapper">

        <header>
            <div id="title-bar">
                {% block page_header %}
                    <h1>MUSER</h1>
                {% endblock %}
            </div>

            <div id="subnav"><ul>
                {% block subnav %}
                {% endblock %}
            </ul></div>
        </header>

        <div id="content-body">
            {% block body %}{% endblock %}
        </div>

        <footer>
            <p><a href="/privacy">Privacy Policy</a></p>
            <p>&copy; 2016 Andrew MacRobert</p>
        </footer>

        {% block javascripts %}{% endblock %}

    </div><!-- /page-wrapper --></body>
</html>
